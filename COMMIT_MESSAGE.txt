feat(nav): 列表下拉菜单暗色风格

修改内容:
- css/custom.css: 追加下拉菜单暗色样式 (774–820)
  前: 无专门覆盖，导致白底
  后: 深色背景、白色文字、hover 提亮、边框/阴影统一暗色
- package.json: 移除 markdown-it-image-size 依赖，避免安装失败
- src/plugins/markdown.js: 移除 markdown-it-image-size 使用，保持渲染正常
- vitest.config.ts: 加载 @vitejs/plugin-vue、限定测试范围、设置 jsdom 环境
- tests/unit/*: 显式引入 vitest 的 test/expect，单元测试通过
- cypress.config.js + cypress/support/e2e.js: 配置支持文件并忽略跨域脚本异常

验证:
- 本地构建成功 (vite build)
- 单元测试通过 (3/3)
- 预览 http://localhost:8000/，悬停“列表”，下拉菜单背景/文字/hover 为暗色

feat(header): 统一各页面顶部背景图 + 导航玻璃态

修改内容:
- css/custom.css: 末尾追加全局 `#page-header` 背景统一为 `/img/autumn-forest.png`，并为未固定导航添加半透明模糊背景
- js/custom.js: 首页默认 `butterfly-top.jpg` 兜底替换为统一图 `/img/autumn-forest.png`

验证:
- 所有页面均加载 `css/custom.css`，新增规则含 `!important` 覆盖各页面内联背景
- 首页已有 `#page-header.full_page` 指向同一图（css/custom.css:960-964）；其余页面由全局规则统一
- 预览 http://localhost:8000/，检查首页/归档/分类/关于/友链 页头均显示统一背景
